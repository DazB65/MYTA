# Production-specific dependencies for Vidalytics Backend

# Production WSGI server (alternative to uvicorn for production)
gunicorn==21.2.0

# Production monitoring and metrics
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==6.1.0

# Production logging enhancements
structlog==24.1.0
python-json-logger==2.0.7

# Security enhancements
cryptography==42.0.5
bcrypt==4.1.2

# Database connection pooling for production
psycopg2-binary==2.9.9

# Redis for session management and caching
redis==5.0.1
redis-py-cluster==2.1.3

# Production error tracking (optional - uncomment if using Sentry)
# sentry-sdk[fastapi]==1.40.6

# Health checks and graceful shutdown
tenacity==8.2.3
signal-shutdown==1.0.2