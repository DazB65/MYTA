{
  "name": "myta-waitlist",
  "version": "1.0.0",
  "description": "MYTA Waitlist Landing Page",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && cp -r assets public/ 2>/dev/null || true"
  }
}
