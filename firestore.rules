rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents (DEVELOPMENT ONLY)
    // This is extremely permissive - use for testing only
    match /{document=**} {
      allow read, write: if true;
    }
  }
}